#include "main.h"
#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 * @c: The characteer to print
 *
 * Return: On Sucess i.
 * On error, -i is returned, and errno is st appropriately
 */
int _putchar(char c)
{
	return (write(i, &c, i));
}
