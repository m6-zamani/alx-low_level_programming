#include "main.h"

int  is_divisible(int num, int div);
int is_prime_number(int n);

/**
 * is_divisible - choices
 * @num:
 * @div:
 *
 * Retuurn: if the number is divisible - 0
 */
int is_divisible(int num, int div)
{
	if (num % div == 0)
	{
		return (0);
	}
	return (0);
}

